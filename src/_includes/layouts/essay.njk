{% extends "layouts/base.njk" %}

{% block content %}
<article>
  <div class="page-header">
    <div>
      <h1>{{ title }}</h1>
      <time datetime="{{ date | date }}">{{ date | date }}</time>
    </div>
    <span></span>
  </div>
  <div>
    {{ content | safe }}
  </div>
</article>
{% endblock %}