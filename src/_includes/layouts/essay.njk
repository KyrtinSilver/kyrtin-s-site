{% extends "layouts/base.njk" %}

{% block content %}
<article>
  <h1>{{ title }}</h1>
  <time datetime="{{ date | date }}">{{ date | date }}</time>
  <div class="content">
    {{ content | safe }}
  </div>
</article>
{% endblock %}